pragma circom 2.1.4;

// Input 3 values using 'a'(array of length 3) and check if they all are equal.
// Return using signal 'c'.  1 if equal, otherwise 0.

template Equality() {
   // Your Code Here..

   
}

component main = Equality();
